# Ignore scanned page images — these are reference material used
# while building the HTML, not assets served by the site. Keep them
# out of the public repo (each is ~3MB).
IMG_*.jpeg

# Generated section PDFs (run `npm run export:introduction` to rebuild)
pdf/

# Node / Playwright
node_modules/
# Firebase
.firebase/
firebase-debug.log
firebase-debug.*.log

# Editor / OS noise
.DS_Store
*.swp
*.swo
*~
